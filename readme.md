# Python Lab 1

## News Article Analyzer

### Overview

This project is a Python program that performs basic text analysis on a news article. It demonstrates the use of Python functions, loops, conditionals, regular expressions, and string manipulation to extract useful information from text. The sample news article provided for the assessment discusses **ACME Inc.'s Apple Pie Master**, an AI-powered machine designed to automate the baking of apple pies.

The program allows a user to:

* Count the number of times a specific word appears in the news article.
* Identify the most common word in the article.
* Calculate the average length of all words.
* Count the number of paragraphs.
* Count the number of sentences.
* Analyze multiple articles using a while loop.

## Features

* Case-insensitive word counting
* Most common word detection
* Average word length calculation
* Paragraph counting
* Sentence counting
* User-friendly command-line interface

## Technologies Used

* Python 3
* `re` (Regular Expressions)
* `collections.Counter`

## Project Structure

```
python-lab-1/
│
├── script.py
└── README.md
```


## Installation

1. Clone the repository:

```bash
git clone https://github.com/keerusandra/python-lab-1.git
```

2. Navigate to the project directory:

```bash
cd python-lab-1
```

## Running the Program

Run the application using:

```bash
python script.py
```

or, depending on your system:

```bash
python3 script.py
```

Follow the prompts to enter a news article and a search word. The program will display the requested text analysis results.

## Concepts Demonstrated

* Functions
* Variables
* String manipulation
* Regular expressions
* Lists
* For loops
* While loops
* Conditional statements
* Python modules

## Learning Outcomes

This project satisfies the following requirements:

* Count occurrences of a specific word
* Identify the most common word
* Calculate average word length
* Count paragraphs
* Count sentences
* Use a `for` loop
* Use a `while` loop
* Use `if/else` conditional statements


